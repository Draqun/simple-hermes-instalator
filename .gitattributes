# Normalize line endings on checkin. Shell scripts MUST stay LF — a CRLF
# checkout (e.g. on Windows) breaks the bash shebang and heredocs.
* text=auto eol=lf
*.sh text eol=lf
*.env text eol=lf
*.md text eol=lf
